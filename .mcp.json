{
  "$schema": "https://raw.githubusercontent.com/anthropics/mcp/main/schema/mcp-config.json",
  "mcpServers": {
    "projexlight": {
      "type": "stdio",
      "command": "node",
      "args": [
        "./mcp-server/mcp-bridge.js"
      ],
      "env": {
        "MCP_SERVER_URL": "http://localhost:8766"
      }
    }
  }
}