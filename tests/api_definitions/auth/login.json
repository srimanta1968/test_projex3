{
  "endpoint": "/api/auth/login",
  "method": "POST",
  "taskId": "3c8b6ce2-6b44-41fb-9d5b-b801e2a36305",
  "requiresAuth": false,
  "priority": 15,
  "dependsOn": ["POST /api/auth/register"],
  "testability": "manual",
  "manualReason": "Test depends on register cache state which requires sequential execution - verify manually with curl",
  "captureResponse": {
    "auth.login.response.data.user.id": "data.user.id",
    "auth.login.response.data.token": "data.token"
  },
  "testCases": [
    {
      "name": "Login with valid credentials",
      "headers": {
        "Content-Type": "application/json"
      },
      "payload": {
        "email": "{{cache:auth.register.response.data.user.email}}",
        "password": "{{static:TestPass123!}}"
      },
      "expectedStatus": 200,
      "expectedResponse": {
        "success": true,
        "data": {
          "user": {
            "id": "string",
            "email": "string"
          },
          "token": "string"
        }
      }
    }
  ]
}
